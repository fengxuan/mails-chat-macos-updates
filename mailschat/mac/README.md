This directory is managed by the private `mails-chat-ios` release workflow.

Published artifacts will appear here:

- `appcast.xml`
- notarized `MailsChatMac-*.zip` archives
- optional release notes files for published versions
